<?xml version="1.0" encoding="utf-8"?>
<!-- Brand-purple launch window with the logo mark centered. Covers the gap
     between the system splash and Flutter's first frame so there is no flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splashBackground" />

    <item android:gravity="center">
        <shape android:shape="rectangle">
            <solid android:color="@color/logoNavy" />
            <corners android:radius="28dp" />
            <size android:width="120dp" android:height="120dp" />
        </shape>
    </item>

    <item
        android:drawable="@drawable/logo_glyph"
        android:gravity="center"
        android:width="120dp"
        android:height="120dp" />
</layer-list>
